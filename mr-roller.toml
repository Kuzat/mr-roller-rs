# Mr Roller configuration.
#
# Environment overrides use config-rs' idiomatic nested key format:
#   MR_ROLLER__DATABASE__URL='sqlite:mr-roller.db?mode=rwc'
#   MR_ROLLER__ADMIN__BOOTSTRAP_ADMIN_IDS='1,42'
#   MR_ROLLER__DISCORD__TOKEN='your-token'
#
# To load a different config file, set:
#   MR_ROLLER_CONFIG='./path/to/mr-roller.toml'

[admin]
# Player IDs that become admins when they run /start.
bootstrap_admin_ids = [119502664126955523]

[database]
# Omit or comment out `url` to use in-memory storage.
url = "sqlite:mr-roller-discord.db?mode=rwc"

[events]
enabled = true
check_interval_seconds = 60
spawn_chance_per_check = 0.004
max_active_events = 1

[events.random_item_spawn]
enabled = true
timeout_seconds = 900

[[events.random_item_spawn.items]]
kind = "regular_dice"
weight = 1

[[events.random_item_spawn.items]]
kind = "reroll_token"
weight = 3

[discord]
enabled = true
# token = ""
# Optional dev guild for instant command registration.
guild_id = 1357697998192119899
# Channel where random events are posted.
home_channel_id = 1499329261717291039
